import 'package:flutter/material.dart';

class LoessaPage extends StatelessWidget {
  const LoessaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Loessa Page"));
  }
}
