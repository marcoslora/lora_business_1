import 'package:flutter/material.dart';

class ColmadosPage extends StatelessWidget {
  const ColmadosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Colmados Page"));
  }
}
